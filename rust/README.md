# Rust – Utilities & Clap CLI

Deutsch:
- CLI-Beispielprojekt: `rust/cli` (Clap)
- Build: `cd rust/cli && cargo build --release`
- Run: `./target/release/codesnips --help`

English:
- CLI sample: `rust/cli` (Clap)
- Build: `cd rust/cli && cargo build --release`
- Run: `./target/release/codesnips --help`
